<UserControl x:Class="Pdp.UI.Controls.Query"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pdp.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding Query}" Grid.RowSpan="4"/>
        <Button Content="Сохранить локально" Command="{Binding SaveLocalCommand}" Grid.Column="1" Margin="3,3,3,0" Padding="5,3,5,3"/>
        <Button Content="Сохранить глобально" Command="{Binding SaveGlobal}" Grid.Row="1" Grid.Column="1" Margin="3,3,3,0" Padding="5,3,5,3"/>
        <Button Content="Собрать и копировать" Command="{Binding BuildPromptCommand}" Grid.Row="2" Grid.Column="1" Margin="3,3,3,0" Padding="5,3,5,3" Background="#FF7FEA85"/>
    </Grid>
</UserControl>
