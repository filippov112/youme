<UserControl x:Class="Pdp.UI.Controls.Search"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pdp.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding Pattern}" Margin="3,3,3,3"/>
        <CheckBox Grid.Row="1" Content="Искать в названиях" IsChecked="{Binding FindByName}" Margin="3,0,3,3"/>
        <CheckBox Grid.Row="2" Content="Искать в содержимом" IsChecked="{Binding FindByContent}" Margin="3,0,3,3"/>
        <Button Content="Найти" Grid.Row="0" Command="{Binding FindCommand}" Grid.Column="1" Margin="0,3,3,3" Padding="5,1,5,1" />
    </Grid>
</UserControl>
