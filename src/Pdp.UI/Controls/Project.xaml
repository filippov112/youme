<UserControl x:Class="Pdp.UI.Controls.Project"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pdp.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox Text="{Binding MainCatalog}" Grid.ColumnSpan="2" IsEnabled="False" Margin="3,3,3,0" Padding="3,3,3,3"/>

        <TextBox Text="{Binding GraphPath}" Grid.Row="1" IsEnabled="False" Margin="3,3,3,0" Padding="3,3,3,3"/>
        <Button Content="Подключить зависимости" Command="{Binding SelectGraphFileCommand}" Grid.Row="1" Grid.Column="1" Margin="3,3,3,0" Padding="5,3,5,3"/>
        <CheckBox Content="Не использовать зависимости" IsChecked="{Binding DisabledDependencies}" Grid.Row="2" Margin="3,3,3,0" />
        <Button Content="Сгенерировать граф зависимостей" Command="{Binding CreateGraphCommand}" Grid.Row="3" Grid.ColumnSpan="2" Margin="3,3,3,0" Padding="5,3,5,3"/>

        <ListBox ItemsSource="{Binding ExternalCatalogs}" Grid.Row="4" Grid.ColumnSpan="2" Margin="3,3,3,3">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Удалить"
                        Command="{Binding DeleteCommand}"
                        CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="Добавить" Command="{Binding AddCommand}"/>
                </ContextMenu>
            </ListBox.ContextMenu>

            <ListBox.ItemTemplate>
                <DataTemplate>

                    <Border Background="Transparent" Padding="5">
                        <TextBlock Text="{Binding Path}" />
                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
